version: 2.1
jobs:
  build:
    docker:
      - image: python:3.6.8-slim-stretch
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            pip3 install -r requirements.txt
      - run:
          name: Run tests
          command: |
            pytest
version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.6.8
  steps:
    - checkout
    - run:
        name: Install dependencies
        command: pip3 install -r requirements.txt
    - run:
        name: Run tests
        command: pytest
